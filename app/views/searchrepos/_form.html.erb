<%= form_with(model: searchrepo, local: true) do |form| %>
  <% if searchrepo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(searchrepo.errors.count, "error") %> prohibited this searchrepo from being saved:</h2>

      <ul>
        <% searchrepo.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
